#include<stdio.h>
int main()
{
    printf("the size of integer is %d\n",sizeof(int));
    printf("the size of float is %d\n",sizeof(float));
    printf("the size of character is %d\n",sizeof(char));
    return 0;
}